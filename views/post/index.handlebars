{{#each errors}}

    <div class="alert alert-danger">{{text}}</div>

    {{else}}

{{/each}}
<div class="card bg-light border-dark mt-4">
    <div class="card-body">
        <h5 class="card-title">{{post.title}}</h5>
        <hr>
            <small>Data da publicação: {{post.creationDate}}</small>
        <br>
        <hr>
        <small>{{post.content}}</small>
    </div>
</div>

<br>